name: 'bump-and-publish'
description: 'Bumps version (or uses manual tag) and runs `deno publish`'
author: 'openbible-io'
inputs:
  NPM_TOKEN:
    required: true
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4 # `bun publish` doesn't support NPM_TOKEN or provenance
    - run: ${{ github.action_path }}/bump-and-publish.sh
      shell: bash
      env:
        NPM_TOKEN: ${{ inputs.NPM_TOKEN }}
